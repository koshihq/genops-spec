apiVersion: v1
kind: Service
metadata:
  name: {{ include "genops-ai.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "genops-ai.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default .Values.deployment.container.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "genops-ai.selectorLabels" . | nindent 4 }}